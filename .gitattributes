# Store vCard files exactly as written (preserve CRLF in the served blob).
# GitHub Pages serves the raw blob, so the bytes must not be normalized.
*.vcf   -text

# HTML/CSS/JS served as-is with LF
*.html  text eol=lf

# Treat images and scripts sensibly
*.png   binary
*.ps1   text eol=crlf
